{
  "mcpServers": {
    "memory-agent": {
      "command": "python",
      "args": ["mcp_server.py", "stdio"],
      "cwd": ".",
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "REDIS_HOST": "localhost",
        "REDIS_PORT": "6379"
      }
    }
  }
}